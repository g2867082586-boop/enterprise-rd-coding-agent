# 登录失败排查手册

当出现 `AUTH001` 时，先确认请求是否包含 username/password 且编码正确，再确认用户存在且 status 为 ACTIVE。随后检查密码哈希算法版本是否完成迁移、认证服务配置是否一致。不得记录密码、令牌或哈希。

当出现 `AUTH002` 时核对冻结原因和审批记录；Agent 无权自动启用账号。出现 `AUTH003` 时检查五分钟失败计数、反向代理真实 IP 和各节点 UTC 时钟。如果需要修改账号或清除限流，必须进入人工确认节点。

