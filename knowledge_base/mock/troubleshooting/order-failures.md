# 订单失败排查手册

订单 `ORDER002` 的确认链路：查最近七天 `FAILED` 订单按错误码分组；若 `ORDER002` 最多，再关联库存服务延迟和锁竞争。当前模拟数据特意让最近七天 `ORDER002` 出现三次、`ORDER003` 出现一次，七天外另有一条 `ORDER002`，用于验证时间边界。

修复优先级：先检查库存服务延迟，再核对预占幂等键，最后评估超时阈值。任何补偿下单、库存写入或订单状态修改都属于高风险写操作，必须人工确认，当前 MVP 不执行。

