# 项目重启与第二版配置指南

> 以下均在 Windows PowerShell 执行。不要用 `.env.example` 覆盖已填写的 `.env`。

## MySQL 模式日常重启

第一个 PowerShell：

```powershell
Set-Location -LiteralPath "D:\AI Agent+MCP从0到1\enterprise-rd-agent"
docker compose up -d mysql
docker compose ps
.\.venv\Scripts\python.exe -m alembic upgrade head
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

第二个 PowerShell：

```powershell
Set-Location -LiteralPath "D:\AI Agent+MCP从0到1\enterprise-rd-agent\frontend"
npm run dev
```

打开 `http://127.0.0.1:5173`。日常重启无需重新执行 `npm install` 或数据种子初始化。

## SQLite + Mock 离线模式

```powershell
Set-Location -LiteralPath "D:\AI Agent+MCP从0到1\enterprise-rd-agent"
$env:DATABASE_PROVIDER="sqlite"
$env:LLM_PROVIDER="mock"
$env:EMBEDDING_PROVIDER="tfidf_fallback"
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

## 本地中文语义 Embedding

在本地 `.env` 设置：

```env
EMBEDDING_PROVIDER=local
EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
EMBEDDING_DEVICE=cpu
```

首次会下载约 90MB 模型。更换模型后必须重建索引：

```powershell
.\.venv\Scripts\python.exe scripts\build_knowledge_base.py
```

## OpenAI-compatible 真实 LLM

仅在本地 `.env` 填写，不要把 Key 发送到对话：

```env
LLM_PROVIDER=openai_compatible
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
ALLOW_MOCK_FALLBACK=true
```

配置后重启后端。健康检查只报告 Key 是否存在，不输出内容：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

## 验证命令

```powershell
.\.venv\Scripts\python.exe scripts\evaluate_v2.py
.\.venv\Scripts\python.exe -m pytest -q
Set-Location frontend
npm test -- --run
npm run build
```
