# 企业知识库 Agent 项目日常启动与停止指令

适用项目目录：

```text
D:\AI Agent+MCP从0到1\enterprise-rd-agent
```

首次安装和数据库初始化已经完成后，日常重新启动项目只需要启动 MySQL、后端和前端。不需要重新安装依赖，不需要重新初始化数据库，也不要覆盖现有 `.env`。

## 一、启动前检查

先手动打开 Docker Desktop，等待 Docker Engine 完成启动。

不要自动安装或修改 Google Chrome，不需要下载 Playwright Chromium。本项目浏览器自动化使用本机 Google Chrome：

```text
channel="chrome"
```

## 二、启动 MySQL 和后端

打开第一个 PowerShell 窗口，执行：

```powershell
Set-Location -LiteralPath "D:\AI Agent+MCP从0到1\enterprise-rd-agent"

docker compose up -d mysql
docker compose ps

.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

`docker compose ps` 的 MySQL 状态应包含：

```text
healthy
```

后端启动成功后应看到类似输出：

```text
Uvicorn running on http://127.0.0.1:8000
```

保持这个 PowerShell 窗口打开。

后端地址：

- 健康检查：`http://127.0.0.1:8000/health`
- Swagger API：`http://127.0.0.1:8000/docs`

也可以另开 PowerShell 验证健康检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

## 三、启动前端

打开第二个 PowerShell 窗口，执行：

```powershell
Set-Location -LiteralPath "D:\AI Agent+MCP从0到1\enterprise-rd-agent\frontend"

npm run dev
```

前端启动成功后应看到类似输出：

```text
Local: http://127.0.0.1:5173/
```

浏览器访问：

```text
http://127.0.0.1:5173
```

新用户可以直接打开注册页面：

```text
http://127.0.0.1:5173/register
```

## 四、停止项目

停止后端：在后端 PowerShell 窗口按：

```text
Ctrl+C
```

停止前端：在前端 PowerShell 窗口按：

```text
Ctrl+C
```

如果还需要停止项目 MySQL，在项目根目录执行：

```powershell
Set-Location -LiteralPath "D:\AI Agent+MCP从0到1\enterprise-rd-agent"

docker compose stop mysql
```

`docker compose stop mysql` 不会删除数据库数据。下次使用时重新执行：

```powershell
docker compose up -d mysql
```

## 五、日常启动时不需要执行的命令

以下命令通常只用于首次安装、配置变更或数据库迁移，不要在每次启动时执行：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e .
.\.venv\Scripts\python.exe scripts\init_database.py
.\.venv\Scripts\python.exe scripts\provision_web_database.py
.\.venv\Scripts\python.exe -m alembic upgrade head
```

前端依赖没有变化时，也不需要重复执行：

```powershell
npm install
```

## 六、重要安全提醒

不要再次执行无条件覆盖 `.env` 的命令：

```powershell
Copy-Item .env.example .env
```

这会清空或覆盖已经配置的数据库密码。已有 `.env` 时应保留原文件，只在需要时手工增加缺失配置。

不要执行：

```powershell
docker compose down -v
```

其中 `-v` 会删除项目的 MySQL 数据卷，可能造成项目数据库数据丢失。

不要把 `.env`、数据库密码、Cookie、Token 或 API Key 发送到对话、日志或代码仓库中。

## 七、常见问题

### Docker 显示无法连接

先确认 Docker Desktop 已启动，并等待 Engine 完成加载，再执行：

```powershell
docker version
docker compose ps
```

### MySQL 不是 healthy

查看项目 MySQL 日志：

```powershell
Set-Location -LiteralPath "D:\AI Agent+MCP从0到1\enterprise-rd-agent"
docker compose logs --tail 100 mysql
```

不要停止或修改本机已有的其他 MySQL 服务。

### 后端提示端口 8000 被占用

检查监听进程：

```powershell
Get-NetTCPConnection -State Listen -LocalPort 8000 -ErrorAction SilentlyContinue
```

先确认是否是此前启动但未关闭的本项目后端，不要直接停止来源不明的进程。

### 前端提示端口 5173 被占用

检查监听进程：

```powershell
Get-NetTCPConnection -State Listen -LocalPort 5173 -ErrorAction SilentlyContinue
```

先确认是否是此前启动但未关闭的本项目前端。

### 页面无法连接后端

分别确认：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

以及前端 PowerShell 是否仍显示 Vite 正在运行。
